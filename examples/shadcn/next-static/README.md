# Next.js static shadcn example

This private example renders a checked-in JSON Render specification with the React shadcn package.
It is built in public CI and is not published to npm.

```bash
pnpm --filter @examples/shadcn-next-static dev
```

The example runs on `http://localhost:3100`. Use it to verify static catalog and registry integration
without an AI provider or API credentials.
